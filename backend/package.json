{
  "name": "@stellar/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit && esbuild src/server.ts --bundle --platform=node --target=node18 --format=cjs --outfile=dist/server.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.1",
    "@hono/zod-validator": "^0.2.0",
    "hono": "^4.6.6",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@stellar/shared": "file:../shared",
    "@types/node": "^20.11.19",
    "esbuild": "^0.23.1",
    "tsx": "^4.7.0"
  }
}
